#include <stdio.h>
#DEFINE MAX_BUF = 1024

int main(int argc, char **argv){
	char buf[MAX_BUF];
	fd = open(argv[1], O_RDONLY);
	if (fd == -1)
		errExit("can't open %s", argv[1]);

	while (charRead = read(fd, buffer, MAX_BUF) > 0)
		








}
